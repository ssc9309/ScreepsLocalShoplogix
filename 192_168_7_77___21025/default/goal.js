//This is just a notepad

/*

auto assign the rangeBuilderLimit depending on the room distance

deploy army if an enemy shows up? (then what about invaders...)



use hashing to clean up the keep up creep number function

room.memory[creepRole]
hash [creepRole: count, etc etc]

let ranged miner put the range builder flag. or create new type of flag (orage or something), use spawn room memory for the flag to use in the keepupcreep number

Hank. the rooms are too far apart now to send over a single army froma single room. they travel through too much stuff.

*/

//launch code
//nuker.launchNuke(new RoomPosition(20,30, 'W1N1'));